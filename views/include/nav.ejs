<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3">
    <a class="navbar-brand metro-logo" href="/">METRO</a>
    <button class=" navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item <%= path === '/movies' || path === '/admin/movies' ? 'active' : '' %>">
                <a class="nav-link" href='<%= isAdminSignedIn ? "/admin/movies" : "/movies"%>'>Movies</a>
            </li>
            <% if(isSignedIn){ %>
            <li class="nav-item <%= path === '/bookings' ? 'active' : '' %>">
                <a class="nav-link" href="/bookings">Bookings</a>
            </li>
            <li class="nav-item <%= path === '/profile' ? 'active' : '' %>">
                <a class="nav-link" href="/profile">Profile</a>
            </li>
            <% } %>
            <% if(isAdminSignedIn){%>
            <li class="nav-item <%= path === '/admin/add-movie' ? 'active' : '' %>">
                <a class="nav-link" href="/admin/add-movie">Add Movie</a>
            </li>
            <% } %>
        </ul>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item mx-3">
                <a href="https://github.com/kareemkasem/metro-cinema" target="_blank" title="view on GitHub"
                    class="normal-link h2 text-light">
                    <i class="fab fa-github"></i>
                </a>
            </li>
            <% if(isSignedIn || isAdminSignedIn){ %>
            <li class="nav-item ml-3">
                <form action='<%= isSignedIn ? "/signout" : isAdminSignedIn ? "/admin/signout" : "/" %>' method="POST">
                    <button class="btn btn-outline-danger" type="submit">sign out</button>
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                </form>
            </li>
            <% } else { %>
            <li class="nav-item mr-3">
                <a href="/signin">
                    <button class="btn btn-outline-success mb-2 mb-lg-0" type="submit">sign in</button>
                </a>
            </li>
            <li class="nav-item">
                <a href="/signup">
                    <button class="btn btn-outline-primary" type="submit">sign up</button>
                </a>
            </li>
            <% } %>
        </ul>
    </div>
</nav>